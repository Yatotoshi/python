Вывести на экран горизонтальную змейку, состоящую из языка (~),
туловища (=), длина которого хранится в переменной,
и хвоста (--).
---
Вывод:
~*=============--


*** Для решения задачи нужно вспомнить, что при умножении
строки s на число n, получаем строку, составленную из
строки s, повторенной n раз. Например, после выполнения кода:
emodjis = ':3 ' * 4
переменная emodjis будет хранить значение ':3 :3 :3 :3 '.